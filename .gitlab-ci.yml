stages:
  - mavenbuild
  - maventest
  
maven-build:            
  stage: mavenbuild
  tags:
    - JunhyungRunner      # <-- change to your runner's tag
  script:
    - cd Backend/mainPackage        # change 'Backend' to to where you have the pom.xml (do not add / in the beginning)
    - mvn package
  only:
    refs:
      - JunhyungBranch_CICD
    changes:
      - Backend/**/*


maven-test:             
  stage: maventest
  tags:
    - JunhyungRunner     # <-- change to your runner's tag
  script:
    - cd Backend/mainPackage       # change 'Backend' to to where you have the pom.xml (do not add / in the beginning)
    - mvn test
  only:
    refs:
      - JunhyungBranch_CICD
    changes:
      - Backend/**/*
